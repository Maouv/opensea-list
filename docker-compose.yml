services:
  opensea-bot:
    build: .
    container_name: opensea-bot
    restart: unless-stopped
    env_file: .env
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
